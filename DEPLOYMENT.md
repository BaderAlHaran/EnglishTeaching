# 🚀 Render Deployment Guide

This guide will help you deploy the Essay Writing Service to Render.

## 📋 Prerequisites

1. **GitHub Repository**: Your code should be in a GitHub repository
2. **Render Account**: Sign up at [render.com](https://render.com)
3. **Domain (Optional)**: You can use Render's free subdomain or add your custom domain

## 🔧 Files Required for Deployment

The following files are already configured for Render deployment:

- `requirements.txt` - Python dependencies
- `wsgi.py` - WSGI entry point for production
- `Procfile` - Process configuration
- `render.yaml` - Render service configuration
- `env.example` - Environment variables template

## 🚀 Deployment Steps

### Step 1: Prepare Your Repository

1. **Commit all changes**:
   ```bash
   git add .
   git commit -m "Prepare for Render deployment"
   git push origin main
   ```

2. **Verify files are present**:
   - ✅ `requirements.txt`
   - ✅ `wsgi.py`
   - ✅ `Procfile`
   - ✅ `render.yaml`
   - ✅ `app.py`
   - ✅ `templates/` directory
   - ✅ Static files (`styles.css`, `script.js`)

### Step 2: Deploy on Render

1. **Go to Render Dashboard**:
   - Visit [render.com](https://render.com)
   - Sign in to your account

2. **Create New Web Service**:
   - Click "New +" → "Web Service"
   - Connect your GitHub repository
   - Select the repository containing your code

3. **Configure Service**:
   - **Name**: `essay-writing-service` (or your preferred name)
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn wsgi:app`
   - **Plan**: Free (or upgrade as needed)

4. **Set Environment Variables**:
   - `FLASK_ENV` = `production`
   - `SECRET_KEY` = (auto-generated by Render)
   - `ADMIN_USERNAME` = `admin` (or your preferred username)
   - `ADMIN_PASSWORD` = (generate a secure password)
   - `ADMIN_EMAIL` = `admin@yourdomain.com`

5. **Deploy**:
   - Click "Create Web Service"
   - Wait for the build to complete (5-10 minutes)

### Step 3: Access Your Application

1. **Get Your URL**:
   - Render will provide a URL like: `https://essay-writing-service.onrender.com`
   - Bookmark this URL for easy access

2. **Test the Application**:
   - Visit the main page: `https://your-app.onrender.com`
   - Test the essay form: `https://your-app.onrender.com/essay-form`
   - Access admin: `https://your-app.onrender.com/login`

## 🔐 Admin Access

After deployment, you can access the admin dashboard:

1. **Go to**: `https://your-app.onrender.com/login`
2. **Login with**:
   - Username: `admin` (or your configured username)
   - Password: The password you set in environment variables

## 📁 File Structure for Render

```
your-project/
├── app.py                 # Main Flask application
├── wsgi.py               # WSGI entry point
├── run.py                # Development runner
├── requirements.txt      # Python dependencies
├── Procfile             # Process configuration
├── render.yaml          # Render configuration
├── env.example          # Environment variables template
├── templates/           # HTML templates
│   ├── admin.html
│   └── login.html
├── uploads/             # File upload directory (auto-created)
├── styles.css           # CSS styles
├── script.js            # JavaScript
├── index.html           # Home page
├── about.html           # About page
├── essay-form.html      # Essay form
├── terms.html           # Terms and conditions
└── privacy.html         # Privacy policy
```

## ⚙️ Environment Variables

| Variable | Description | Example |
|----------|-------------|---------|
| `FLASK_ENV` | Environment mode | `production` |
| `SECRET_KEY` | Flask secret key | Auto-generated |
| `ADMIN_USERNAME` | Admin username | `admin` |
| `ADMIN_PASSWORD` | Admin password | `your-secure-password` |
| `ADMIN_EMAIL` | Admin email | `admin@yourdomain.com` |
| `PORT` | Server port | `10000` (auto-set by Render) |

## 🔧 Troubleshooting

### Common Issues:

1. **Build Fails**:
   - Check `requirements.txt` has all dependencies
   - Verify Python version compatibility

2. **App Won't Start**:
   - Check `wsgi.py` is properly configured
   - Verify `Procfile` has correct command

3. **Database Issues**:
   - SQLite database is created automatically
   - Admin user is created on first run

4. **File Upload Issues**:
   - Uploads folder is created automatically
   - Check file size limits (16MB max)

### Logs and Debugging:

1. **View Logs**:
   - Go to your service dashboard on Render
   - Click "Logs" tab to see real-time logs

2. **Common Log Messages**:
   - `✅ Database initialized successfully` - Database setup complete
   - `🔐 Admin Username: admin` - Admin credentials (development only)
   - `* Running on all addresses` - Server started successfully

## 🚀 Post-Deployment

### 1. Test All Features:
- [ ] Home page loads correctly
- [ ] Essay form submission works
- [ ] Admin login works
- [ ] File uploads work
- [ ] All pages are accessible

### 2. Security Checklist:
- [ ] Admin password is secure
- [ ] Environment variables are set
- [ ] Debug mode is disabled in production
- [ ] CORS is properly configured

### 3. Performance:
- [ ] Static files load quickly
- [ ] Database queries are efficient
- [ ] File uploads work within limits

## 🔄 Updates and Maintenance

### Updating Your App:
1. Make changes to your code
2. Commit and push to GitHub
3. Render will automatically redeploy

### Database Backups:
- SQLite database is stored in Render's filesystem
- Consider upgrading to PostgreSQL for production
- Regular backups recommended for important data

## 📞 Support

If you encounter issues:

1. **Check Render Logs**: Look for error messages
2. **Verify Configuration**: Ensure all environment variables are set
3. **Test Locally**: Run `python run.py` to test locally first
4. **Contact Support**: Use Render's support system

## 🎉 Success!

Once deployed, your Essay Writing Service will be live and accessible to users worldwide!

**Your app URL**: `https://your-app-name.onrender.com`
**Admin URL**: `https://your-app-name.onrender.com/login`
